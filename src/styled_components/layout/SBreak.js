import styled from 'styled-components';

const SBreak = styled.hr`
	box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
	width: 80%;
	height: 0.25rem;
	border: none;
	margin-top: 0;
`;

export default SBreak;
